# Scripts

## Available tools

- `benchmark/`   Automated runner captures hardware info and produces schema-conformant JSON output.
- `validation/`  Schema checks for JSON benchmarks plus CSV format validation testing.
- `data/`      Data manipulation and output formatting
