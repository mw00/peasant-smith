# Model Profiles

Information about AI models

Include quant levels tested, hardware compatibility notes, and known issues per model.

## Adding a profile
PR with new YAML file under `models/<family>/` following existing conventions.