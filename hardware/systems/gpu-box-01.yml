# System profile: gpu-box-01
# Canonical registry: data/systems.csv - the `hardware_ref` field in benchmark
# records joins against the system_slug there.

slug: gpu-box-01
role: primary benchmark box
introduced: 2026-08

cpu:
  model: Intel Xeon E5-1650 v4
  architecture: Broadwell-EP
  cores: 6
  threads: 12
  base_clock_ghz: 3.6
  boost_clock_ghz: 4.0

memory:
  system_ram_gb: 128
  type: DDR4 UDIMM desktop
  speed_mhz: 2400
  channels: 4
  notes: quad-channel via X99 (Machinist) motherboard

gpus:
  - model: NVIDIA GeForce RTX 3060
    count: 2
    vram_gb_each: 12
    architecture: Ampere

storage:
  device: WD Blue SN570 1TB (WDC WDS100T2B0C)
  type: NVMe SSD

os: Ubuntu 26.04 LTS
driver: "610.43.02"

notes:
  - Used-parts reference build (the "peasant build").
  - 128 GB RAM lets MoE models over 100B spill from VRAM and still run interactively.
