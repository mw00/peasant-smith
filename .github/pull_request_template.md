## Summary

Brief description of what this PR adds or changes.

## Checklist

- [ ] Benchmark JSON validates against schema v1.0 (`scripts/validation/validate_benchmark.py`)
- [ ] `example` key set correctly (`true` for examples, `false` for real results)
- [ ] All required fields populated per schema
